function Button() {
    const styles = {
        backgroundColor: "hsl(200, 75%, 25%)",
        color: "white",
        padding: "0.5em 1em",
        borderRadius: "0.5rem",
        border: "none",
        cursor: "pointer",
    };

    return <button style={styles}>Click me</button>;
}
export default Button;
